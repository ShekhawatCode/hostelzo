import { apiControllerBlogController } from './api-controller-blog-controller';

describe('apiControllerBlogController', () => {
  it('should work', () => {
    expect(apiControllerBlogController()).toEqual(
      'api-controller-blog-controller'
    );
  });
});
