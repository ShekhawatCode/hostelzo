import { apiControllerAuthController } from './api-controller-auth-controller';

describe('apiControllerAuthController', () => {
  it('should work', () => {
    expect(apiControllerAuthController()).toEqual(
      'api-controller-auth-controller'
    );
  });
});
