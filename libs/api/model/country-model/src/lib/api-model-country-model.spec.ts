import { apiModelCountryModel } from './api-model-country-model';

describe('apiModelCountryModel', () => {
  it('should work', () => {
    expect(apiModelCountryModel()).toEqual('api-model-country-model');
  });
});
