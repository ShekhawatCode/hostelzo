import { apiModelStaticContentModel } from './api-model-static-content-model';

describe('apiModelStaticContentModel', () => {
  it('should work', () => {
    expect(apiModelStaticContentModel()).toEqual(
      'api-model-static-content-model'
    );
  });
});
