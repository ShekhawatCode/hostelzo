import { apiModelAdminUserModel } from './api-model-admin-user-model';

describe('apiModelAdminUserModel', () => {
  it('should work', () => {
    expect(apiModelAdminUserModel()).toEqual('api-model-admin-user-model');
  });
});
